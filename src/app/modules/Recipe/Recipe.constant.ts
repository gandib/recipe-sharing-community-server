import { TContentType, TRecipeStatus } from './Recipe.interface';

export const contentType: TContentType[] = ['free', 'premium'];
export const status: TRecipeStatus[] = ['published', 'unpublished'];
